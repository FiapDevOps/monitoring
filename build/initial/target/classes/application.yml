server:
  port: 8080
    
spring:
  application:
    name: gs-spring-boot-docker
    
management:
  endpoints:
    web:
      exposure:
        include: '*'
  metrics:
    export:
      prometheus:
        enabled: true
  endpoint:
    metrics:
      enabled: false
    prometheus:
      enabled: true 